import {
  // methods,
  // method1,
  originalArray,
  doubleArray,
  evensOnlyArray,
  arraySum,
  allNumbersGreaterThanZero,
  sortArray,
  someNumbersAreOdd,
} from "../app";
// hint of look in before_each before_all
// describe("originalArray Test", () => {
//   test("originalArray should return it self", () => {
//     expect(originalArray([1, 2, 3, 4])).toEqual([1, 2, 3, 4]);
//   });
// });
// // return array with double values in array
// doubleArray(numberArray); // => [2,4,6,8];  hint use map
// describe("doubleArray function", () => {
//   test("it should double values in array", () => {
//     expect(doubleArray([1, 2, 3, 4])).toEqual([2, 4, 6, 8]);
//   });
// });

// // return array with all even numbers
// evensOnlyArray(numberArray); // => [2,4];  hint use filter
// describe("filter function", () => {
//   test("return array all even numbers", () => {
//     expect(evensOnlyArray([1, 2, 3, 4])).toEqual([2, 4]);
//   });
// });

// // return array with sum of numbers
// arraySum(numberArray); // => 10;  hint use reduce
// describe("reduce function", () => {
//   test("return array with sum of numbers", () => {    
//     expect(arraySum([2, 3, 5])).toEqual(10);
//   });
// });

// return true if every item greater than 0
// allNumbersGreaterThanZero(numberArray); // => true; use every
// describe("every item greater than zero", () => {
//   test("return all numbers greater than zero", () => {
//     expect(allNumbersGreaterThanZero([0, 1, 2, 3])).toEqual([1, 2, 3]);            --------not done--------
//   });
// });

// // sort array desc order
// sortArray(numberArray); //=> [4,3,2,1] *desc*
// describe("sort array", () => {
//   test("return all desc order", () => {
//     expect(sortArray([1, 2, 3, 4])).toEqual([4, 3, 2, 1]);
//   });
// });

// // return true if one or more items in array are odd
// someNumbersAreOdd(numberArray); // => true; use some
// describe("return true if odd", () => {
//   test("return all thats odd", () => {
//     expect(someNumbersAreOdd([1, 2, 3, 4, 5, 6])).toEqual([2, 4, 6])  --not done--
//   });
// });